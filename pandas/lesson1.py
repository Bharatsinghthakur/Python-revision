# we need pandas to install pandas we have to run -- 

#python -m pip install notebook  -- IT will install the notebook globally 
                                    #or 
# pip install notebook

# to start the notebook we use  -- Jupyter notebook 

